# Angular

Fuse does not have built-in code generation support for Angular yet. However, you can access the GraphQL API you create with Fuse with **any GraphQL client that works on Angular!**

For example, [Apollo Angular](https://the-guild.dev/graphql/apollo-angular/docs) is a popular option.