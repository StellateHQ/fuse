# iOS (Swift)

Fuse does not have built-in code generation support for iOS yet. However, you can access the GraphQL API you create with Fuse with **any GraphQL client that works on iOS!**

For example, [Apollo Client for iOS](https://www.apollographql.com/docs/ios/) is a popular option.