# Android (Java/Kotlin)

Fuse does not have built-in code generation support for Android yet. However, you can access the GraphQL API you create with Fuse with **any GraphQL client that works on Android!**

For example, [Apollo Kotlin](https://www.apollographql.com/docs/kotlin) is a popular option.