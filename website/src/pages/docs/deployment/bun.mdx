# Deploying your Fuse API with Bun

First, build your API server into a single JS file that can be run with Bun:

```sh
fuse build --adapter bun
```

This will build your server into `build/bun.js`, which you can deploy to any hosting provider that supports running JS with Bun.
